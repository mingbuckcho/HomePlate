apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarqube-pvc
  namespace: cicd
spec:
  accessModes:
    - ReadWriteOnce
  # [수정] AWS EBS gp3 스토리지 클래스 사용
  storageClassName: gp3 
  resources:
    requests:
      storage: 20Gi # 데이터 증가를 대비해 조금 늘렸습니다.