# HTTP 요청
requests==2.32.3

# HTML 파싱
beautifulsoup4==4.12.3
lxml==5.3.0              # BeautifulSoup의 HTML 파서 (기본 html.parser보다 빠름)

# MongoDB 드라이버
pymongo==4.9.1

# MariaDB 드라이버
pymysql==1.1.1

# 날짜/시간 처리
python-dateutil==2.9.0.post0

# AWS S3 클라이언트
boto3==1.35.0

# Kafka 클라이언트 (librdkafka C 라이브러리 기반, 프로덕션 표준)
confluent-kafka==2.4.0

# MSYQL 인증을 위한 패키지
cryptography>=42.0.0
